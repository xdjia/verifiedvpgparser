// From ANTLR's JSON grammar
// '([^']+)'
// "$1"

json : value ;

obj : "{" pair ("," pair)* "}" | "{" "}" ;

pair : STRING ":" value ;

arr : "[" value ("," value)* "]" | "[" "]" ;

value
   : STRING
   | NUMBER
   | obj
   | arr
   | "true"
   | "false"
   | "null"
   ;

